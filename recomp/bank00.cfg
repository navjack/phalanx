# Phalanx (USA), LoROM bank $00.
#
# The vectors are architectural roots. The whole-program analyzer emits only
# variants it can prove; all remaining execution stays live through LLE.

bank = 0
auto_vectors
tier_down_stubs

# The reset handler enters this permanent main loop after hardware setup.
# Naming it makes runtime coverage and generated manifests readable without
# claiming the loop is a host-callable function.
func MainLoop 8845

